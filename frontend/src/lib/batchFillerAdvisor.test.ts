import { describe, expect, it } from "vitest";
import { planRouteFillSuggestions } from "@/lib/batchFillerAdvisor";
import type { BatchRouteFillerSuggestionRow, FlipResult } from "@/lib/types";
const row=(o:Partial<FlipResult>)=>({TypeID:1,TypeName:'A',BuySystemID:10,BuyLocationID:100,SellSystemID:20,SellLocationID:200,...o} as FlipResult);
const suggestion=(o:Partial<BatchRouteFillerSuggestionRow>)=>({type_id:1,type_name:'A',units:1,unit_volume_m3:1,buy_system_id:10,buy_location_id:100,sell_system_id:20,sell_location_id:200,volume_m3:10,added_profit_isk:1000,added_capital_isk:100,fill_confidence:0.7,stale_risk:0.1,suggested_role:'safe_filler',filler_score:80,...o});
describe('batchFillerAdvisor',()=>{it('filters selected and confidence',()=>{const out=planRouteFillSuggestions({rows:[row({}),row({TypeID:2,BuyLocationID:101,SellLocationID:201})],selectedLineKeys:['1:100:200'],remainingCargoM3:20,suggestions:[suggestion({}),suggestion({type_id:2,buy_location_id:101,sell_location_id:201,fill_confidence:0.4})]});expect(out).toHaveLength(0)});it('supports zero-volume and ranks',()=>{const out=planRouteFillSuggestions({rows:[row({TypeID:3,BuyLocationID:102,SellLocationID:202}),row({TypeID:4,BuyLocationID:103,SellLocationID:203})],selectedLineKeys:[],remainingCargoM3:5,suggestions:[suggestion({type_id:3,buy_location_id:102,sell_location_id:202,volume_m3:0,added_profit_isk:3000,filler_score:90}),suggestion({type_id:4,buy_location_id:103,sell_location_id:203,volume_m3:5,added_profit_isk:2000,stale_risk:0.6})]});expect(out[0].type_id).toBe(3)})})
